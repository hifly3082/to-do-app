import logo from '../assets/logo.png'

const Logo = () => {
  return (
    <img
      src={logo}
      alt='logo'
      height={50}
      width={50}
      style={{ marginLeft: '-32px' }}
    />
  )
}
export default Logo
